import styled from '@emotion/styled';
import { IStyledProps } from './EventDetails.types';

export const Text = styled.p``;

export const Deadline = styled.span``;

export const Status = styled.span<IStyledProps>``;
