const enum QueryKeys {
  events = 'events',
  monthlyEvents = 'monthlyEvents',
  token = 'token',
  user = 'user',
  isLoggedIn = 'isLoggedIn',
  eventById = 'eventById',
}

export default QueryKeys;
