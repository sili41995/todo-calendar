import Calendar from '@/components/Calendar';

const App = () => {
  return (
    <div
      style={{
        width: 1200,
        marginLeft: 'auto',
        marginRight: 'auto',
        padding: 20,
      }}
    >
      <Calendar />
    </div>
  );
};

export default App;
